apiVersion: apps/v1
kind: Deployment
metadata:
  name: noteflow-deployment
  labels:
    app: noteflow
spec:
  replicas: 2
  selector:
    matchLabels:
      app: noteflow
  template:
    metadata:
      labels:
        app: noteflow
    spec:
      volumes:
        - name: noteflow-data
          persistentVolumeClaim:
            claimName: noteflow-pvc
      containers:
        - name: noteflow
          image: tanmaydixit09/noteflow-app:latest
          ports:
            - containerPort: 5000
          env:
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: noteflow-secret
                  key: secret-key
            - name: FLASK_ENV
              value: "production"
            - name: NOTEFLOW_DB
              value: "/app/data/noteflow.db"
            - name: FLASK_DEBUG
              value: "0"
          volumeMounts:
            - name: noteflow-data
              mountPath: /app/data
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"

